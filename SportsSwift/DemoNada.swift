//
//  DemoNada.swift
//  SportsSwift
//
//  Created by nada elmasry on 2/3/22.
//  Copyright © 2022 nada elmasry. All rights reserved.
//

import Foundation


/*
 Ana msh 3rfa ha7ot al code da f2anhy 7eta bal zabt hom mmkn yab2a al func al bn3rf feha al cell
 wbasta5dam condition da anhy collection view akararo 3 marat aw anady 3al api al mnasba wa 2ahandel 7ett al concatenation
 //MARK:---------------------Code For ViewController the should return data but it is not
 //Teams:-
 let api : TeamsAPIModelProtocol = TeamsAPIModel()
 api.getData { (result) in
     switch result
     {
     case .success(let response):
         let Teams = response?.TeamsArr
         for team in Teams ?? []
         {
             print("\(String(describing: team.strTeam))\n")
         }
     case .failure(let error):
         print(error.localizedDescription)
         print("\nFailed to fetch all teams data in League Details")
     }
 }
 
 //-------------------------------------------------------------------------------------
 
 //UpcomingEvents
 
 //MARK:---------------------Code For ViewController the should return data but it is not
 
 let api : UpcomingEventsNetworkingAPIProtocol = UpcomingEventsNetworkingAPI()
 api.getData { (result) in
     switch result
     {
     case .success(let response):
         let UpEvents = response?.AllEventsFromAPI
         for UpEvent in  UpEvents?? []
         {
             print("\(String(describing: UpEvent.strTeam))\n")
                //hena a7ot ay field mn gowa
         }
     case .failure(let error):
         print(error.localizedDescription)
         print("\nFailed to fetch all UpcomingEvents in League Details")
     }
 }
 
 //-------------------------------------------------------------------------------------
 


//Latest Events

//MARK:---------------------Code For ViewController the should return data but it is not

let api : LatesetEventsNetworkingAPIProtocol = LatesetEventsNetworkingAPI()
api.getData { (result) in
    switch result
    {
    case .success(let response):
        let UpEvents = response?.AllEventsFromAPI
        for RecEvent in  UpEvents?? []
        {
            print("\(String(describing: RecEvent.strTeam))\n")
               //hena a7ot ay field mn gowa
        }
    case .failure(let error):
        print(error.localizedDescription)
        print("\nFailed to fetch all UpcomingEvents in League Details")
    }
}

//-------------------------------------------------------------------------------------
*/
//lazm a7awal al kalam kolo mvp --ashoof al video tany
//al ui btA3 al screen al2a5era
//al testing
//adding to the favourite table
//checking conactivity
//indicator
//pop up alert messges
//constraintssss

